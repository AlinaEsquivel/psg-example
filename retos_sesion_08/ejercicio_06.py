print ("")

#Coordenadas
coordenadas1 = (-20, -20)
x1, y1 = coordenadas1
print ("coordenadas 1:", coordenadas1)

coordenadas2 = (40, 20)
x2, y2 = coordenadas2
print ("coordenadas 2:", coordenadas2)

#Encontrar punto medio de x - y
punto_medio_x = (x1 + x2) / 2
punto_medio_y = (y1 + y2) / 2

#almacenar en tupla
tupla = (punto_medio_x, punto_medio_y)
print ("punto medio", (tupla))

